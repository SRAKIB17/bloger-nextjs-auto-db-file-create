import React from 'react';
import NotFound from '../../components/BLOG/NotFound';

const index = () => {
    return (
        <div>
            <NotFound />
        </div>
    );
};

export default index;