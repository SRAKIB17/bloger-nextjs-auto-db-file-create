import React from 'react';
import htmlTags from '../hooks/hooks/useHtmlTags';

const Docs = () => {

    return (
        <div>
        {/* {
            htmlTags.map(tag=><div>{tag.des}</div>)
        } */}
        </div>
    );
};

export default Docs;